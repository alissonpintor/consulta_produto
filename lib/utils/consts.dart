import 'package:flutter/material.dart';

const baseUrl = 'https://treina.stoky.dev.br/mge/service.sbr';

// Main Colors
const cardBackgroundColor = Color(0xFF21222D);
const primaryColor = Color(0xFF2697FF);
const secondaryColor = Color(0xFFFFFFFF);
const backgroundColor = Color(0xFF15131C);
const selectionColor = Color(0xFF88B2AC);

// Side Menu Colors
const sideMenuBackGround = Color.fromRGBO(23, 24, 33, 1);
const sideMenuItemSelected = Colors.black;

// Paddings
const defaultPadding = 20.0;
